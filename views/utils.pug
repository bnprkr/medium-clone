mixin validationErrorSummary(errors)
  if errors 
    div 
      p The following errors occurred: 
      ul 
        each error in errors 
          li= error

mixin field(labelText, fieldName, fieldValue, fieldType = 'text')
  div
    label(for=fieldName)= labelText
    input(type=fieldType id=fieldName name=fieldName value=fieldValue)

mixin storyPreviews(stories)
  each story in stories 
    article 
      .story-box 
        .story-author 
          p #{story.author}
        .story-title 
          h2 #{story.title} 
        .story-text
          each para in story.sample.split('\n')
            p #{para}
        .story-box-bottom-bar 
          .left 
            .story-likes
              span #{story.numLikes} Likes
          .right 
            .story-comments 
              span #{story.numComments} Comments